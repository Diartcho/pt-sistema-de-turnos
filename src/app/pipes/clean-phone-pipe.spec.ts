import { CleanPhonePipe } from './clean-phone-pipe';

describe('CleanPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new CleanPhonePipe();
    expect(pipe).toBeTruthy();
  });
});
